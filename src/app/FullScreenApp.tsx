import React from 'react';
import { FullScreenBox } from '../components/FullScreenBox/FullScreenBox';

export const FullScreenApp = (props: {
  type: 'selector' | 'theoretical' | 'real-world';
}) => {
  return <FullScreenBox>a</FullScreenBox>;
};
