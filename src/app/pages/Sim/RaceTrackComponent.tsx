import React from 'react';

type RaceTrackComponentProps = {
  width: number;
  height: number;
};
export const RaceTrackComponent = (props: RaceTrackComponentProps) => {
  return (
    /*
    <EuclideanCanvas
      width={props.width}
      height={props.height}
      boundingBox={{
        ...calcBoundingRect(sessionState.locations),
        paddingMarginRatio: PADDING_MARGIN_RATIO,
      }}
      setViewportCenter={setSessionViewportCenter}
      onDragStart={onDragStart}
      onDragEnd={onDragEnd}
      onDrag={onDrag}
      onFocus={onFocus}
      onUnfocus={onUnfocus}
      onPointerUp={onPointerUp}
      onClearSelection={onClearSelection}
      onMoved={onMoved}
      draggingIndex={uiState.draggingIndex}
      sessionState={sessionState}
      viewportCenter={uiState.viewportCenter}
      selectedIndices={uiState.selectedIndices}
      focusedIndices={uiState.focusedIndices}
      matrices={matrices}
    />
     */
    <></>
  );
};
